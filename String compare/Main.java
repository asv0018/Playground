#include<stdio.h>
int main()
{
  char klover[4],nisha[4];
  scanf("%s",&klover);
  scanf("%s",&nisha);
  if(strcmp(klover,nisha))
     printf("Strings are same");
  else
     printf("Strings are same");
}